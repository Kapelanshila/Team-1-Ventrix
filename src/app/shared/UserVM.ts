export interface UserVM {
    userId: Number
    description : string 
    userRoleId : Number 
    registered: boolean
    hashedPassword:string
    name: String,
    surname: String,
    emailAddress:String,
}